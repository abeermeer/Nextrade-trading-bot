services:
  backend:
    volumes:
      - .:/app
    command: uvicorn web.main:app --host 0.0.0.0 --port 8000 --reload

  analyst:
    volumes:
      - .:/app
    command: python scripts/run_analyst.py

  trader:
    volumes:
      - .:/app
    command: python scripts/run_trader.py
